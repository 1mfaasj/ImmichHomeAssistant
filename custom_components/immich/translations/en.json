{
  "config": {
    "abort": {
      "already_configured": "Integration already configured"
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "Invalid authentication",
      "unknown": "Unexpected error"
    },
    "step": {
      "user": {
        "data": {
          "api_key": "API key",
          "host": "Host"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "watched_albums": "Albums to create entities for",
          "refresh_interval": "Refresh interval (seconds)",
          "no_repeat_window": "Avoid repeating the last N images",
          "tag_filter": "Tag filter (comma-separated)",
          "shuffle_mode": "Shuffle mode",
          "random_speed": "Random slideshow speed"
        }
      }
    }
  }
}